<div class="row">
  <div class="col-auto">
    <h2>Welcome, {{name}}!</h2>
  </div>
</div>

<div class="row mt-4">
  <div class="col-md-6">
    <h3>Goated Promotes:</h3>
    <ul class="goated_posts">
      {{#each posts}}
    <li>{{this.title}}</li>
    <li>{{this.description}}</li>
    <li>{{this.socMed}}</li>
      {{/each}}
    </ul>
