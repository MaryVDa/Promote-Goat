<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name = "viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <title>Promote Goat 🐐</title>
    
    </head>

<body>
    <div class = "flex-column justify-space-around">
        <h1>Promote Goat 🐐</h1>

        <nav>
            <a class = "button is-dark" id = "home-btn" href="/">Login</a>
            {{#if logged_in}}
                <a href="/profile" class="button is-dark" id="profile-btn">Profile</a>
                <a href="/post" class="button is-dark" id="create-post-btn">Create Promote</a>
                <a href="/timeline" class="button is-dark" id="all-posts">See All Promotes</a>
            {{else}}
            <a class = "button is-dark" id = "home-btn" href="/">Logout</a>
            {{/if}}
        </nav>

        <main class="container container-fluid mt-5">
            {{{body}}}
        </main>

        <footer class="footer is-">
            <div class="content has-text-centered">
                <p>
                    <strong>© 2023 Promote Goat 🐐</strong>
                </p>
        </footer>


    </div>

{{!-- Render the script only if the user is logged in. If the user is logged in, render the logout button. If the user is not logged in, render the login button. --}}

{{#if logged_in}}
    <script src="./js/logout.js"></script>
{{else}}
    <script src="./js/login.js"></script>
{{/if}}

</body>





</html>