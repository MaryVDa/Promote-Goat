<link rel="stylesheet" href="/css/post.css">

<div class="row mt-4">
  <div class="col-md-6">
    <h3>🐐Create a New Promote:</h3>

    <form class="form new-post-form">
      <div class="form-group">
        <label for="post-title">Promote Title:</label>
        <input class="form-input" type="text" id="post-title" name="post-title" />
      </div>
      <div class="form-group">
        <label for="post-desc">Description:</label>
        <textarea class="form-input" id="post-desc" name="post-desc"></textarea>
      </div>
      <div class="form-group">
        <label for="project-socMed">Social Media:</label>
        <textarea class="form-input" id="post-socMed" name="post-socMed"></textarea>
      </div>
      {{!-- <div class="form-group">
        <label for="post-image">Image:</label>
        <input class="form-input" type="file" id="post-image" name="post-image" accept="image/*"/>
      </div> --}}
      <div class="form-group">
        <button type="submit" class="btn btn-primary">Create Promote</button>
      </div>
    </form>
  </div>

  {{!-- {{#if post.length}}
  <div class="col-md-6 post-list">
    <h3>Current Promotes:</h3>

    {{#each posts as |post|}}
    <div class="row mb-2">
      <div class="col-md-8">
        <h4><a href="/post/{{post.id}}">{{post.name}}</a></h4>
      </div>
      <div class="col-md-4">
        <button class="btn btn-sm btn-danger" data-id="{{post.id}}">DELETE</button>
      </div>
    </div>
    {{/each}}
  </div>
  {{/if}}
</div>

<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{description}}</p>
  <p>{{socMed}}</p>
  <p>{{image}}</p>
  <p>
    <button class="btn btn-primary">🐐Promote🐐</button>
  </p>
</div> --}}

<script src="./js/post.js"></script>